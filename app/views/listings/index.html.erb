<h1>AirBnB Clone Listings</h1>

<ul>
  <% @listings.each do |listing| %>
    <li><%= link_to listing.title, listing_path(listing) %></li>
  <% end %>
</ul>

<%= link_to "New Listing", new_listing_path %>
